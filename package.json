{
  "name": "nerf-herders",
  "version": "0.1.0",
  "license" : "MIT license",
  "private": true,
  "scripts": {
    "client": "cd client && yarn serve",
    "server": "cd server && yarn start",
    "setup": "concurrently \"cd server && yarn install\" \"cd client && yarn install\"",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
